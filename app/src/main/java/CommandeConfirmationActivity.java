import android.os.Bundle;
import android.widget.TextView;
import androidx.appcompat.app.AppCompatActivity;
//import fr.isen.kaddouri.droidburger.databinding.ActivityCommandeConfirmationBinding;

class CommandeConfirmationActivity {

        /*j'ai tenté ce code mais ça n'a pas abouti j'y été pourtant preseque...*//*
        override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState);
        val binding = ActivityCommandeConfirmationBinding.inflate(layoutInflater);
        setContentView(binding.root);

        // Récupérer les données passées depuis l'activité principale
        val nom = intent.getStringExtra("NOM");
        val prenom = intent.getStringExtra("PRENOM");
        val adresse = intent.getStringExtra("ADRESSE");
        val numero = intent.getStringExtra("NUMERO");
        val burgerSelectionne = intent.getStringExtra("BURGER_SELECTIONNE");

        // Accéder aux vues via findViewById
        findViewById<TextView>(R.id.textViewNom).text = "Nom: $nom";
        findViewById<TextView>(R.id.textViewPrenom).text = "Prénom: $prenom";
        findViewById<TextView>(R.id.textViewAdresse).text = "Adresse: $adresse";
        findViewById<TextView>(R.id.textViewNumero).text = "Numéro de téléphone: $numero";
        findViewById<TextView>(R.id.textViewBurger).text = "Burger sélectionné: $burgerSelectionne";
        */

        }